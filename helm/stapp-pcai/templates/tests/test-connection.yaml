apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "stapp.fullname" . }}-test-connection"
  labels:
    {{- include "stapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "stapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
